-- Seed file for development
-- Note: Deal stages are seeded per-user via the seed_deal_stages() function
-- which should be called after user signup (e.g., from a server action on first login)

-- Example: SELECT seed_deal_stages('your-user-uuid-here');
